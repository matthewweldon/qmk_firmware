#pragma once

// place overrides here

//Adding below 1ms delay allows you to use the KC_VOLU instead of mac specific K__VOLU on the encoders
//For some reason KC_VOLU doesnt behave nicely otherwise on the encoder when used
#define TAP_CODE_DELAY 1
